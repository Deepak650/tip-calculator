apiVersion: v1
kind: Service
metadata:
  name: tip-calculator
  labels:
    app: tip-calculator
spec:
  selector:
    app: tip-calculator
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP  # Use either LoadBalancer or ClusterIP, not both
